# --- App Configuration ---
# The IP address the Flask app will bind to.
# Use 0.0.0.0 to be accessible from other machines on the network, or your Tailscale IP.
FLASK_HOST=0.0.0.0

# The port for the Flask app.
FLASK_PORT=5000

# --- Security ---
# A long, random string for Flask session security. It's required by Flask to secure sessions.
# You can generate one with this command: python3 -c 'import secrets; print(secrets.token_hex(24))'
SECRET_KEY=
